{
  "version": "2.0.0",
  "classification_rules": [
    {
      "rule": "Set dataSourceMode to uploaded_file when a workbook is attached and the requested outcome concerns that workbook; otherwise use existing_db for canonical PMO reporting."
    },
    {
      "rule": "Use inspect_file only for workbook structure, sheet, column, or type inspection."
    },
    {
      "rule": "Use review_staging for mapping, normalization, or staged-row review without a canonical DB diff."
    },
    {
      "rule": "Use validate when the requested outcome emphasizes readiness, data quality, missing references, parse errors, or duplicates."
    },
    {
      "rule": "Use preview_changes when the user asks what canonical DB rows would be inserted, updated, skipped, or overwritten without publishing."
    },
    {
      "rule": "Use publish only when the user explicitly asks to import, publish, apply, sync, or update canonical PMO data."
    },
    {
      "rule": "Use generate_report when the user asks for idle, overbook, utilization, or PMO reporting without requesting publication."
    },
    {
      "rule": "Use publish_then_report only when the user explicitly requests both publication and report generation."
    },
    {
      "rule": "Choose the narrowest actionMode satisfying the explicit requested outcome. Never add canonical writes implicitly."
    },
    {
      "rule": "Extract dates and report types only when explicitly present. Never invent dates."
    }
  ]
}
